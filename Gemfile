source "https://rubygems.org"

# Matches the gem set GitHub Pages uses to build your site, so local
# previews behave like production. Run: bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

# Lock down a Jekyll version GitHub Pages supports.
gem "jekyll", "~> 3.9"